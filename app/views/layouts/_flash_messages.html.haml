- flash.each do |name, msg|
  %div{:class => "alert #{bootstrap_class_for(name)} alert-dismissible", role: 'alert'}
    %button.close{'aria-label' => 'Close', 'data-dismiss' => 'alert', :type => 'button'}
      %span{'aria-hidden' => 'true'} ×
    %strong= msg
